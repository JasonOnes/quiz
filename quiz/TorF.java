package org.launchcode.quiz;

import org.launchcode.quiz.MultipleChoice;

public class TorF extends MultipleChoice {

    public TorF(int numChoices, String prompt, int[] answer, int numPicks) {
        super(2, prompt, answer, 1);
    }
    TorF() {

    }
}
